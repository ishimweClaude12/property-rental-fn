import { createAction } from '@ngrx/store';

export const toggleDropdown = createAction('[Header Component] Open Dropdown');
export const closeDropdown = createAction('[ Home Component] Close Dropdown');
